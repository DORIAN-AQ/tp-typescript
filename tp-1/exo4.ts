const array: number[] = [1, 2, 3, 4];

function displayArray(arr) {
    // Pas le droit à la boucle for/while
    // Utilisez la méthode map ou forEach d'un object de type array
}

displayArray(array);
