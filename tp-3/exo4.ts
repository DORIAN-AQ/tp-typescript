namespace tp3_exo4 {

    // =================================
    // NE PAS TOUCHER
    // =================================

    const listOfNumber: number[] = [1, 2, 3, 4, 5];
    // =================================

    // Déclarer et instancier : a, b et c depuis les valeurs de `listOfNumber` afin d'afficher sous ce format :
    // a = 2
    // b = 1
    // c = [3,4,5]

    // Replace me by your code

    // =================================
    // NE PAS TOUCHER
    // =================================
    console.log(`a = ${a}`);
    console.log(`b = ${b}`);
    console.log(`c = [${c}]`);
    // =================================

    // On va réattribué les variable : a et b depuis la valeur `c` afin d'afficher sous ce format :
    // a = 3
    // b = 4

    // =================================
    // NE PAS TOUCHER
    // =================================
    console.log(`a = ${a}`);
    console.log(`b = ${b}`);
    // =================================
}