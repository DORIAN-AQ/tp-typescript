namespace tp3_exo3 {

    // Compléter la fonction suivante afin d'afficher et d'instancier uniquement la propriété `firstName`
    function displayFirstName(): void {

    };

    // Compléter la fonction suivante afin d'utiliser et d'instancier uniquement la propriété `age` pour signaler si c'est l'age légal pour pouvoir acheter de l'alcool
    function canBuyAlcoolInFrance(): boolean {

    };

    // =================================
    // NE PAS TOUCHER
    // =================================

    const profil = {
        lastName: 'Dylan',
        firstName: 'Bob',
        age: 78
    };

    displayFirstName(profil);
    canBuyAlcoolInFrance(profil);

    // =================================
}